import HeroTitle from "./HeroTitle/HeroTitle";

const Hero = () => {
  return (
    <section className="bg-accent">
      <div className="p-3.5">
        <HeroTitle />
      </div>
    </section>
  );
};

export default Hero;
