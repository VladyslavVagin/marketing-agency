import { FC } from "react";
import { Project } from "@/types/project";

const Information: FC<Project> = ({ project }) => {
  return <div className="bg-white relative z-20">Information</div>;
};

export default Information;
